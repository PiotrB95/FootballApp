export enum TabName {
  Players = 'players',
  Teams = 'teams',
  Games = 'games',
  Stats = 'stats',
}

export enum ModeName {
  Light = 'light',
  Dark = 'dark',
}

export enum DateOption {
  DAY = 'day',
  WEEK = 'week',
  MONTH = 'month',
}
